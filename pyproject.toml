[project]
name = "email-triage-openenv"
version = "0.1.0"
description = "OpenEnv email triage environment"
authors = [{name = "Vansh"}]
dependencies = [
    "pydantic",
    "fastapi",
    "uvicorn",
    "python-multipart"
]
